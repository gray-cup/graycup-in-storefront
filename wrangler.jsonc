{
  "$schema": "node_modules/wrangler/config-schema.json",
  "main": "./workers/app.ts",
  "name": "graycup-in-storefront",
  "compatibility_date": "2026-08-08",
  "compatibility_flags": [
    // Enable Node.js API (Drizzle/Neon serverless driver need this)
    // see https://developers.cloudflare.com/workers/configuration/compatibility-flags/#nodejs-compatibility-flag
    "nodejs_compat",
    // Populate process.env from Worker bindings/secrets (needed since src/lib/db.ts reads process.env.DATABASE_URL)
    // see https://developers.cloudflare.com/workers/configuration/compatibility-flags/#nodejs-compatibility-flag
    "nodejs_compat_populate_process_env",
    // Allow to fetch URLs in your app
    // see https://developers.cloudflare.com/workers/configuration/compatibility-flags/#global-fetch-strictly-public
    "global_fetch_strictly_public",
  ],
  "assets": {
    "directory": "./build/client",
    "binding": "ASSETS",
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "graycup-orders",
      "database_id": "c761625d-96b2-40a9-b1a3-4d9a58cae436",
    },
  ],
  "observability": {
    "enabled": true,
    "logs": {
      "enabled": true,
      "invocation_logs": true,
    },
  },
  "vars": {
    "SITE_URL": "https://graycup.in",
  },
  // Per-IP rate limits for the custom API routes (best-effort, per Cloudflare
  // location). period must be 10 or 60. See src/lib/rate-limit.ts.
  "ratelimits": [
    {
      // checkout, subscription create, coupon validation
      "name": "API_RATE_LIMITER",
      "namespace_id": "1001",
      "simple": { "limit": 20, "period": 60 },
    },
    {
      // unauthenticated content writes: reviews, contact form
      "name": "STRICT_RATE_LIMITER",
      "namespace_id": "1002",
      "simple": { "limit": 5, "period": 60 },
    },
  ],
}
